# Production Requirements for HackRx 6.0 Document Intelligence System
# Optimized for Render deployment with Python 3.11.9 compatibility

# Core FastAPI and web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# Document processing
PyMuPDF>=1.24.0  # Updated version with Python 3.13 wheels
python-docx==1.1.0
pandas>=2.2.0  # Updated for Python 3.13 compatibility
pdfplumber==0.10.0
Pillow>=10.2.0

# AI and API integration
openai>=1.12.0
# tiktoken>=0.5.0  # Commented out - has Rust dependencies that cause build issues

# Vector database - using standard client to avoid Rust compilation
pinecone>=4.0.0
numpy>=1.24.0  # Updated for Python 3.13 compatibility

# HTTP requests and utilities
requests==2.31.0
python-dotenv==1.0.0

# Optional table extraction (may not work on all deployment platforms)
# camelot-py[cv]==0.11.0  # Commented out for deployment compatibility
# opencv-python==4.8.1.78  # Commented out for deployment compatibility
# tabula-py==2.8.2  # Commented out for deployment compatibility

# Production optimizations for Render
# Using uvicorn as ASGI server (already included above)